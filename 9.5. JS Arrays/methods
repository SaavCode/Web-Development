//arrays jave built in methods
//push  and pop
var colors = ["red", "blue" ];
//add green to the end of the array
colors.push ("green");
//removes last element of the array
colors.pop();
//colors.unshift("yellow"); //add to front of the array
//remove from front
colors.shift();
//indexOf, find the index of an item
//check if element is an array
colors.indexOf("red");
//cut the array
var light = colors.slice(1,3);
//copy colors array
var colors2 = colors.slice();