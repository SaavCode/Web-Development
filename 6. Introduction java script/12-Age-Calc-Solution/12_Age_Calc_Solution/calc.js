// ask user for the age
var age = prompt("What is your age?")
// set the age tmultiplied by days in a year
var days = age * 365.25;
// output the age
alert(age + " years is roughly " + days + " days");